# Ollama Shim Service Environment Variables
#
# Create a .env file in this directory for actual configuration.
# All variables are optional as they have defaults defined in code.

# LM Studio connection settings (change if not using default port/host)
LM_STUDIO_URL=http://localhost:1243
AUTH_TOKEN=  # Add your authentication token if required

# Model endpoints - only change if your models are at different locations
PRIMARY_MODEL_URL=http://localhost:1234/v1/chat/completions
REFINER_MODEL_URL=http://localhost:1234/v1/chat/completions